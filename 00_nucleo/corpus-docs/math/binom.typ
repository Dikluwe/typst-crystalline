// Corpus docs-derived (Passo 998) — página: https://typst.app/docs/reference/math/binom/
// Cada caso cita a frase exacta da documentação que o motiva.

#set page(width: auto, height: auto, margin: 1cm)
#set text(font: "New Computer Modern", size: 11pt)

// Fonte: https://typst.app/docs/reference/math/binom/
// Citação: "A binomial expression."
$ binom(n, k) $

// Fonte: https://typst.app/docs/reference/math/binom/
// Citação: "A binomial expression."
$ binom(n, k_1, k_2, k_3, ..., k_m) $

// Caso derivado da prosa, sem exemplo na doc:
// Citação: "The binomial's upper index."
$ binom(x + y, 2) $

// Caso derivado da prosa, sem exemplo na doc:
// Citação: "The binomial's lower index."
$ binom(5, 1, 2) $
