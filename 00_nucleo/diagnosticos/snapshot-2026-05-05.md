# Snapshot 2026-05-05 — Estado pré-M8 (reconciliado)

**Etiqueta**: "Snapshot de transição — M8 nova sessão"
(reconciliado por P202).
**Data nominal**: 2026-05-05.
**Data de reconciliação**: 2026-05-05 (P202).
**Versão**: pós-P202 (reconciliada com realidade empírica).
**Pré-versão (conceptual)**: `00_nucleo/snapshot-2026-05-05.pre-P202.md`.
**Auditoria de origem**: `00_nucleo/diagnosticos/typst-passo-201-auditoria-delta.md`.
**Registo de reconciliação**: `00_nucleo/diagnosticos/typst-passo-202-reconciliacao.md`.

**Marco arquitectural**: M5 universal completo + M6 fechado
+ M7 estruturalmente fechado + M9 11/11 + F1 fechado +
F3 parcial. **M8 (comemo) é próximo passo arquitectural
natural** para paridade vanilla typst.

---

## §1 Identificação

- **Estado nominal**: consolidação arquitectural intermédia.
- **Origem**: agregação incremental P156B → P200C + P190
  série + P191 série + P192 série.
- **Sub-passo final relevante**: P200B (M5 universal) +
  P190I (M6) + P192B (M7).

---

## §2 Cobertura empírica

- **Tests workspace**: **1823 verdes** (verificado
  empíricamente 2026-05-05; baseline P156A 1145; Δ=+678).
- **Crystalline-lint**: **0 violations** (verificado
  `crystalline-lint .`).
- **Comemo**: versão 0.4 declarada em `Cargo.toml`
  workspace; uso intermédio (não nativo) — paridade
  comemo nativa é objectivo M8.

---

## §3 Estado de marcos arquitectónicos

| Marco | Estado | Sub-passo final |
|-------|--------|-----------------|
| **M1** (Walk emite tags + payload) | ✅ fechado | P163 |
| **M2** (is_locatable função pública) | ✅ fechado | P164 |
| **M3** (Introspector + sub-stores + from_tags) | ✅ fechado | P165 |
| **M3 location-aware** | ✅ fechado | P185E (ADR-0068 ACEITE) |
| **M4** (expose Introspector) | ✅ fechado | P166 |
| **M4-residual** (consumers C1/C2/C3 migrados) | ✅ fechado funcionalmente | P188B |
| **M5 incremental** (1 arm migrado) | ✅ fechado | P189B (Outline) |
| **M5 universal** | ✅ fechado | **P200B** (2026-05-04) |
| **M6** (eliminação `CounterStateLegacy`) | ✅ fechado | **P190I** (2026-05-05; ADR-0070 ACEITE) |
| **M7** (loop fixpoint runtime) | ✅ estruturalmente fechado | **P192B** (2026-05-05; ADR-0072 ACEITE) |
| **M9** (Introspection vanilla) | ✅ 11/11 completo | P182F |
| **F1** (Layouter slim — `counter` elim) | ✅ fechado | P190I |
| **F3** (Layouter -1 field; outros pendentes) | ⚠️ parcial | P190I |
| **M8** (comemo nativo) | ⏳ pendente | próximo passo arquitectural natural |

---

## §4 Estado das ADRs no ciclo M5/M6/M7

### ADRs ACEITES estritas (status formal `ACEITE` no ficheiro): **6**

| ADR | Tópico | Aceite em |
|-----|--------|-----------|
| **0066** | Introspection runtime adiada | P192B (com nota "intermediário até M8") |
| **0068** | Layouter location-aware (M3) | P185E |
| **0069** | Post-recursion tag emission | P195E |
| **0070** | Eliminação `CounterStateLegacy` | P190I |
| **0071** | Walk pipeline com Introspector | P191C |
| **0072** | M7 fixpoint runtime estruturalmente fechado | P192B |

### ADRs EM VIGOR (status formal `EM VIGOR` no ficheiro): **2**

| ADR | Tópico | EM VIGOR desde |
|-----|--------|----------------|
| **0064** | Smart→Option/default (4 casos) | P156K |
| **0065** | Inventariar primeiro (6 critérios) | P156K |

### ADRs PROPOSTAS pendentes: **3**

| ADR | Tópico | PROPOSTA em | Notas |
|-----|--------|-------------|-------|
| **0061** | Layout Fase X roadmap | P156B | mantém PROPOSTO por decisão humana 2026-04-25 |
| **0062** | hayagriva (parsing bibliography) | reserva P156B; ADR formal em ADR-0062-create | PROPOSTO |
| **0067** | Attribute grammar scoping | P185+ (data 2026-05-02) | PROPOSTO; transição → IMPLEMENTADO planeada quando primeira propriedade alvo materializada |

### ADR-0063: slot conceptual vazio

Não existe ficheiro físico. Reservado conceptualmente para
column flow (descoberto em P160A: número 0017 ocupado;
usar 0066). P202 mantém slot vazio (não cria ficheiro
ADR-0063 — ruído sem benefício; convenção implícita
aceite e documentada no registo de reconciliação P202 §7).

### Total ADRs novas no ciclo P156B-P200: **11**

(0061, 0062, 0064, 0065, 0066, 0067, 0068, 0069, 0070,
0071, 0072 — slot 0063 vazio.)

### Correcção retroactiva — ADR-0067 status

**Erro detectado**: P190 consolidado §1 linha 28, P192A
relatório linha 212, P192 consolidado linhas 155, 257,
339, P192B spec linhas 57, 98 listam ADR-0067 como
ACEITE.

**Realidade**: ficheiro
`00_nucleo/adr/typst-adr-0067-attribute-grammar-scoping.md`
linha 3 declara `Status: PROPOSTO`; plano de validação
linha 240 declara: "ADR-0067 transita de `PROPOSTO` para
`IMPLEMENTADO` quando todas estas condições forem
verdadeiras: 1. Pelo menos uma propriedade alvo
(provavelmente `numbering_active`) materializada com o
pattern. 2. Walk recebe o parâmetro de atributos herdados
sem violar P163. 3. Tests E2E confirmam scoping léxico
funciona. 4. Comparação de magnitude com Caminho 2
hipotético confirma que Caminho 3 é praticável."

**Nenhuma das 4 condições foi cumprida**. ADR-0067 é
PROPOSTO formal.

**Decisão P202**: manter PROPOSTO; consolidados anteriores
incorreram em erro propagado; P202 não promove ADRs
unilateralmente. Promoção é trabalho de P203+ se for o
caminho.

---

## §5 Estado estrutural pós-P192B (verificado empíricamente)

### Trait `Introspector` — 20 métodos

Local: `01_core/src/entities/introspector.rs` linhas 40-163.

```text
query_by_kind, query_by_label, query_first, query_unique,
position_of, figure_number_for_label, query_metadata,
formatted_counter, state_value, state_final_value, query,
formatted_counter_at, bib_entry_for_key, bib_number_for_key,
is_numbering_active, figure_number_at_index,
is_numbering_active_at, flat_counter_at,
resolved_label_for, headings_for_toc.
```

`position_of` retorna `Option<()>` (stub estrutural;
materialização concreta de `Position` adiada para M8 ou
sub-passo dedicado de lacuna #1).

### `TagIntrospector` — 9 sub-stores

Local: `01_core/src/entities/introspector.rs` linha 173.

| # | Field | Tipo | Origem |
|---|-------|------|--------|
| 1 | `labels` | `LabelRegistry` | M3 (P165) |
| 2 | `counters` | `CounterRegistry` | M3 (P165) |
| 3 | `kind_index` | `HashMap<ElementKind, Vec<Location>>` | M3 (P165) |
| 4 | `figure_label_numbers` | `HashMap<Label, usize>` | M5 sub-passo 2 (P168) |
| 5 | `metadata` | `MetadataStore` | M9 sub-passo 1 (P169) |
| 6 | `state` | `StateRegistry` | M9 sub-passo 3 (P171) |
| 7 | `bib_store` | `BibStore` | P181B |
| 8 | `resolved_labels` | `ResolvedLabelStore` | P193B |
| 9 | `headings_for_toc` | `Vec<(Label, Content, usize)>` | P200B |

Comentário interno: `// positions: HashMap<Location, Position> — adiado para M5/M9.`

### `ElementPayload` — 12 variants

Local: `01_core/src/entities/element_payload.rs` linha 34.

```text
Heading, Figure, Citation, Metadata, State, StateUpdate,
Outline, Bibliography, Equation, Labelled, CounterUpdate,
HeadingForToc.
```

Nota: spec P202 C4 indicava 13 variants; valor empírico
**12**. Discrepância marginal — provavelmente expectativa
de spec incluía variant Reference ou Equivalente
posteriormente fundido.

### `ElementKind` — 10 variants

Local: `01_core/src/entities/element_kind.rs`. **CONFIRMADO
10**.

### `Content` enum — 60 variants

Local: `01_core/src/entities/content.rs` linha 41.

Nota: spec P202 C4 indicava 13 variants; valor empírico
**60**. Spec C4 estava incorrecta (provavelmente referia
um subset). 60 é o valor real, espalhado por: math (15+),
layout (10+), structure (5+), introspection (10+), refs
(5+), etc.

### `LayouterRuntimeState` — 3 fields

Local: `01_core/src/entities/layouter_runtime_state.rs`
linha 36.

```text
label_pages: HashMap<Label, usize>      // page tracking
known_page_numbers: HashMap<Label, usize>  // fixpoint
is_readonly: bool                        // outline render
```

**CONFIRMADO 3**.

### `Layouter` — 22 fields, sem `counter`

Local: `01_core/src/rules/layout/mod.rs` linha 69.

```text
metrics, sizer, font_size_pt, style, chain, page_config,
pages, current_items, cursor_x, cursor_y, line_start_x,
current_line, introspector, figure_progress,
is_height_unconstrained, cell_available_h, cell_origin_x,
cell_origin_y, cell_origin_w, locator, current_location,
runtime.
```

**Correcção empírica**: 22 fields (não 19 como o snapshot
anterior declarava). Causa: P185C adicionou `locator` +
`current_location` (+2 fields M3 location-aware); P190C/D
adicionaram `runtime: LayouterRuntimeState` (+1 field
agregador). `counter: CounterStateLegacy` field eliminado
em P190I (✅).

### Walk fn — 7 parâmetros

Local: `01_core/src/rules/introspect.rs` linha 714.

```rust
pub(crate) fn walk(
    content:            &Content,
    locator:            &mut Locator,
    tags:               &mut Vec<Tag>,
    intr:               &mut TagIntrospector,
    auto_label_counter: &mut usize,
    lang:               Option<&Lang>,
    label_from_parent:  Option<&Label>,
);
```

**CONFIRMADO 7**.

### Helpers privados família ADR-0069

3 activos (`compute_labelled` linha 377, `compute_heading_auto_toc`
linha 441, `compute_heading_for_toc` linha 489) + 1
eliminado (`compute_figure` orphan eliminado em P190H).

**CONFIRMADO 3 + 1 eliminado**.

### 2 loops fixpoint

| # | Local | Constante | Finalidade |
|---|-------|-----------|------------|
| 1 | `01_core/src/rules/introspect/fixpoint.rs:33` | `MAX_FIXPOINT_ITERATIONS = 5` | `run_fixpoint` opt-in para stdlib features |
| 2 | `01_core/src/rules/layout/mod.rs:1506` | `MAX_ITERATIONS = 5` | TOC fixpoint forward refs page numbers |

**CONFIRMADO 2 loops complementares, MAX=5 paridade
nominal vanilla**.

---

## §6 Estado activo vs preservado

### Activado (pré-existente confirmado pós-P192A)

- 2 loops fixpoint (`fixpoint.rs` + `layout/mod.rs:1515`).
- 4 queries runtime location-aware em Layouter
  (`current_location` desde P185C).
- Walk pipeline com `&mut TagIntrospector` (P191B).
- Sub-stores 9 em `TagIntrospector`.
- Trait `Introspector` 20 métodos.
- Tests E2E cobrem ambos loops (13+ tests
  `fixpoint.rs`).

### Preservado

- ADR anteriores ao ciclo (0001-0060) inalteradas.
- Convenção L0-puro / sufixo `*A` / sub-passos `*B+`.
- Ficheiro `entities/counter_state_legacy.rs` (35 linhas;
  body vazio; preservado para histórico arquivístico do
  prompt-hash). Re-export `CounterUpdate` como
  `CounterAction` preservado para callers.

### Pendente para M8

- Adopção `comemo::Track` em trait `Introspector`.
- Queries location-aware com tracking granular.
- Re-walks parciais via invalidação cross-iteration.
- Saída `cristalino == vanilla` validada por snapshot
  tests.

---

## §7 Lacunas (estado pós-P200C)

| Lacuna | Estado | Último passo | Bloqueia M8? |
|--------|--------|--------------|--------------|
| **#1** (Position) | residual | P165 (stub `Option<()>`) | provavelmente sim |
| **#1b** (Position-related) | residual | depende de #1 | depende #1 |
| **#2** (Counter at locations) | parcial | P185B / P187B+ (API existe; uso location-aware activado) | parcial — M8 deve refinar |
| **#3** (headings_for_toc) | ✅ fechada | P200B | não |
| **#4** (numbering_active StyleChain-like) | ✅ fechada | P182F | não |
| **#5** (CounterRegistry hierárquico) | ✅ fechada | P170 | não |
| **#6** (Bibliography full-stack) | ✅ fechada | P181I | não |
| **#7** (Outline locatable) | ✅ fechada | P178 | não |

**Apenas #1 e #1b residuais**. **#2 parcial**. **#3-#7
fechadas no ciclo**.

---

## §8 M8 (comemo) — escopo

### Objectivo arquitectural

Paridade vanilla typst:
- **Saída igual** ao vanilla (validação por snapshot
  tests em corpus de paridade).
- **Performance comparável** (memoização
  cross-iteration; re-walks parciais).

### Mecanismo

- Adopção `#[comemo::track]` em trait `Introspector`
  (20 métodos).
- Queries location-aware re-emitidas com tracking
  granular.
- Sub-stores `TagIntrospector` (9) tracked
  selectivamente.

### Magnitude esperada

**L cross-modular** (similar a M6).

### Pré-condição

**CUMPRIDA** — M5+M6+M7 estruturalmente fechados;
mecanismo hash-based convergence existente é fallback
viável durante transição.

### Baseline empírico para M8

- Tests workspace: **1823 verdes** (não 1.802).
- Layouter: **22 fields** (não 19); F3 parcial cobre
  21 fields ortogonais (não 18) — `counter` eliminado.
- ADRs ACEITES no ciclo: **6** (não 7); ADR-0067 PROPOSTO
  formal.
- Aplicações diagnóstico-primeiro: ~35 (range plausível
  com spec 33).

---

## §9 Caminhos ortogonais (fora de M8)

- **F3 completo** — refactor restantes 21 fields ortogonais
  do Layouter (não 18). Lower priority.
- **Lacunas residuais #1, #1b, #2** — passos dedicados.
- **Pausa estratégica** — consolidar marcos fechados antes
  de M8.
- **Promoção formal de ADR-0067** — quando primeira
  propriedade alvo (provavelmente `numbering_active`) for
  materializada com pattern attribute-grammar.
- **F2 completo** (não documentado como fechado;
  necessita auditoria).

---

## §10 Convenções e padrões consolidados

### Padrões emergentes pós-P156A

1. **ADR-0069 com 5 variantes operacionais** — post-recursion
   tag emission para resolver "extract_payload puro sem
   state"; família 4 helpers paralelos.
2. **ADR-0070 — eliminação write paralelo** — 8 aplicações
   sequenciais P190B-I.
3. **ADR-0071 — walk pipeline com Introspector** — resolveu
   barreira P190F; from_tags eliminado (-969 LOC).
4. **Layouter-runtime → struct dedicada** — 2 aplicações
   (P190C, P190D).
5. **Auditoria sobre estado existente** (P192A) — variante
   reflexiva do diagnóstico-primeiro.
6. **Substitution-with-fallback** — 7 aplicações
   sequenciais (P168, P181G, P182D, P184D, P187B, P188B,
   P194B).
7. **Auditor #1** — fixture vs restrição (P185D, P191B,
   P200B).
8. **Pause-resume documentado** — P190F→P191→P190G.
9. **Trabalho híbrido sem nova variante** (P200B).

### Convenções operacionais activas

- Sub-passo `.A` = auditoria L0 / diagnóstico.
- Sub-passos `*A` = diagnóstico-primeiro formal.
- Sub-passos `*B+` = implementação sem condicionais.
- 3 outputs padrão por passo.
- Ficheiros relatório-consolidado por série (P181+).
- Sem código Rust nas specs.
- Distinção fecho estrutural vs arquitectural.
- Preservação histórica de relatórios.
- Numeração ADRs sem reuso (slot 0063 mantém-se vazio).
- Pattern auditoria-2-eixos para gates substanciais.
- Pattern absorção pelo consolidado (relatórios individuais
  podem ser absorvidos).

### Palavras banidas / inflação retórica

`patamar`, `limiar`, `consolidação`, `deriva`, `subpadrão`
(como bandeira), `cumulativo` (como bandeira),
`cross-domínio` (como bandeira), `paridade observable`
(como bandeira).

---

## §11 Métricas cumulativas

| Métrica | Valor empírico 2026-05-05 |
|---------|:--:|
| Tests workspace | **1823 verdes** |
| ADRs total | **70** (-1 slot 0063) |
| ADRs ACEITES no ciclo M5/M6/M7 | **6** estritas |
| ADRs EM VIGOR no ciclo | **2** (0064, 0065) |
| ADRs PROPOSTAS pendentes ciclo | **3** (0061, 0062, 0067) |
| ADRs novas no ciclo | **11** (0061, 0062, 0064-0072) |
| Aplicações diagnóstico-primeiro consecutivas | **~35** (33-35 plausível) |
| Crystalline-lint violations | **0** |
| Trait `Introspector` métodos | **20** |
| `TagIntrospector` sub-stores | **9** |
| `ElementPayload` variants | **12** |
| `ElementKind` variants | **10** |
| `Content` enum variants | **60** |
| `LayouterRuntimeState` fields | **3** |
| `Layouter` fields | **22** (sem `counter`; F3 parcial) |
| Walk fn parâmetros | **7** |
| Helpers família ADR-0069 | **3 activos + 1 eliminado** |
| Loops fixpoint complementares | **2**, MAX=5 |
| LOC líquido série P190 | **-990** |
| LOC eliminados from_tags::from_tags | **-969** |
| Marcos fechados | **9** (M1-M7, M9, F1) + F3 parcial |

---

## §12 Diferenças face à versão pré-P202

Resumo das alterações:

| Item | Pré-P202 (declarado) | Pós-P202 (empírico) |
|------|:--:|:--:|
| Tests workspace | 1.802 | **1823** |
| ADRs ACEITES no ciclo | 7 (incluía 0067 erradamente) | **6** estritas |
| ADR-0067 status | ACEITE (consolidados) | **PROPOSTO** (correcção retroactiva) |
| Layouter fields | 19 | **22** (sem `counter`) |
| F3 ortogonais pendentes | 19 | **21** |
| Aplicações diag-1º | 33 | **~35** (range plausível) |
| `TagIntrospector` sub-stores | 9 (parcial; 2 a confirmar) | **9 listadas explicitamente** |
| ADR-0063 | "outra crate reservada" | **slot conceptual vazio** (não ficheiro) |
| Marcos no snapshot | parcial | **M1, M2, M3, M3 location-aware, M4, M4-residual, M5 incremental, M5 universal, M6, M7, M9, F1, F3 parcial** |
| Lacuna #2 | residual | **parcial** |

---

## §13 Resumo para abrir nova sessão

> Cristalino atinge consolidação arquitectural intermédia
> — M5+M6+M7+M9 fechados estruturalmente; F1 fechado;
> F3 parcial. **M8 (comemo) é próximo passo arquitectural
> natural** para paridade vanilla typst (saída igual +
> performance comparável).
>
> **Estado de partida (verificado empíricamente
> 2026-05-05)**:
> - 6 ADRs ACEITES no ciclo M5/M6/M7 (0066, 0068, 0069,
>   0070, 0071, 0072); 2 EM VIGOR (0064, 0065);
>   3 PROPOSTAS pendentes (0061, 0062, 0067).
> - 1823 tests workspace verdes.
> - 0 crystalline-lint violations.
> - ~35 aplicações diagnóstico-primeiro consecutivas.
> - Struct `CounterStateLegacy` eliminada (P190I).
> - Walk fn 7 parâmetros (`+intr +auto_label_counter +lang
>   -state`).
> - Trait `Introspector` 20 métodos; `TagIntrospector`
>   9 sub-stores; `ElementPayload` 12 variants;
>   `LayouterRuntimeState` 3 fields.
> - **Layouter 22 fields**, sem `counter` (F3 parcial cobre
>   21 fields ortogonais; refactor F3 completo
>   pendente).
> - 2 loops fixpoint complementares (TOC + run_fixpoint),
>   MAX=5.
> - Comemo 0.4 declarado em workspace; uso intermédio
>   (não nativo).
>
> **Magnitude esperada M8**: L cross-modular (similar a
> M6).
>
> **Pré-condição cumprida**: M5+M6+M7 estruturalmente
> fechados.
>
> **Trabalho concreto esperado em M8**:
> 1. ADR dedicada (futura ADR-0073 ou similar) — adopção
>    `comemo::Track`.
> 2. `#[comemo::track]` em trait `Introspector` (20
>    métodos).
> 3. Queries location-aware re-emitidas com tracking
>    granular.
> 4. Re-walks parciais via invalidação cross-iteration.
> 5. Validação saída `cristalino == vanilla` (snapshot
>    tests em corpus de paridade).
> 6. Performance comparável.
>
> **Lacunas residuais**:
> - #1 (Position) — residual; potencial bloqueio para
>   queries location-aware completas.
> - #1b (Position-related) — depende de #1.
> - #2 (Counter at locations) — parcial; M8 deve refinar.
> - #3-#7 fechadas no ciclo.
>
> **Caminhos alternativos ortogonais**:
> - F3 completo (21 fields ortogonais).
> - Lacunas residuais (#1, #1b, #2 dedicados).
> - Promoção formal de ADR-0067 (quando primeira
>   propriedade alvo materializada).
> - Pausa estratégica.
