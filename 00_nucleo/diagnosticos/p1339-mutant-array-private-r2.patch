--- r1/01_core/src/compiler/eval/call_dispatch.rs
+++ r2/01_core/src/compiler/eval/call_dispatch.rs
@@ -16,6 +16,10 @@
 // Real isolated negative/control routes; never copied to productive sources.
 #[path = "p1339_mutant_array.rs"]
 mod p1339_mutant_array;
+#[cfg(test)]
+mod p1339_array_owner_binding {
+    include!("/repos/Antigravity/typst-crystalline/00_nucleo/diagnosticos/p1339-mutant-array-owner-binding-r2.rs");
+}
 
 use crate::compiler::scopes::Scopes;
 use crate::compiler::stdlib::{
--- r1/01_core/src/compiler/eval/p1339_mutant_array.rs
+++ r2/01_core/src/compiler/eval/p1339_mutant_array.rs
@@ -5,7 +5,7 @@
 pub(super) fn mode() -> u8 {
     let value = std::env::var("P1339_ARRAY_MUTANT").unwrap_or_else(|_| "0".into());
     let parsed = value.parse::<u8>().expect("invalid isolated Array mutant mode");
-    assert!(parsed <= 12, "invalid isolated Array mutant mode");
+    assert!(parsed <= 13, "invalid isolated Array mutant mode");
     parsed
 }
 
@@ -36,7 +36,7 @@
     remaining.remove_positional(0).expect("real first positional Bytes");
     if mode != 8 {
         if let Some(extra) = remaining.occurrence_sequence().first() {
-            let span = if mode == 9 || extra.span.is_detached() { remaining.span } else { extra.span };
+            let span = if mode == 13 { extra.span } else if mode == 9 || extra.span.is_detached() { remaining.span } else { extra.span };
             let message = match &extra.name {
                 Some(name) => format!("unexpected argument: {name}"),
                 None => "unexpected argument".into(),
