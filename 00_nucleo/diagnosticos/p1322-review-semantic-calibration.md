# P1322 — adaptação focal do leitor TSV de D

O recibo `p1322-review-semantic-r0.json`, em 2026-09-08T22:42:16.391954+00:00, conservou 12 violações de `FUNCTIONAL_SOURCE_BINDING`: somente expressões multilinha. O gerador C (`p1322-classification-final.cjs:210`) serializa cada célula TSV substituindo tab/CR/LF por espaço. O checker inicial comparava essa representação de exibição diretamente com a fonte bruta, e por isso confundiu a exibição em uma linha com troca de fonte.

O parser de D passa a aplicar **somente à comparação da exibição TSV** a mesma projeção dos caracteres tab/CR/LF para espaço. Não altera slash/backslash nem sequências `\\n` literais; não modifica o catálogo, o JSON das sentinelas, argv, source SHA-256, stderr, spans, transições ou critérios de igualdade. Fontes integrais e hashes continuam autenticados pelas matrizes, que são as referências do ledger. O snapshot inicial está em `p1322-review-semantic-r0.py`.

O controle focal é a mesma coorte de 12 linhas com fonte completa disponível; D07/D08 continuam exigindo fonte/ID/prova ligados corretamente. Esta é a primeira revisão da causa de serialização TSV, distinta do bug anterior de serialização JSON de um set. Não houve mudança dos inputs recebidos: os hashes consumidos no recibo R0 coincidem com o congelamento C posterior à correção temporal. A primeira emissão C com 2231/2487 não é objeto de aprovação; sua perda de snapshot é declarada em `p1322-classification-correction.md`.

R1 reduziu 12 violações para uma (`r4.context.calibration`); recibo compacto `p1322-review-semantic-r1.json` e snapshot `p1322-review-semantic-r1.py`. A investigação focal demonstrou que `csv.DictReader` tentava interpretar aspas de uma célula que começava por uma expressão Typst string como quoting CSV. O formato de C é TSV sem quoting. R2 usa `quoting=csv.QUOTE_NONE`, preservando literalmente aspas e backslashes da expressão. O controle focal lê a mesma célula com ambos os modos e mostra que somente QUOTE_NONE recupera os bytes exibidos. Não muda expectativa da linguagem nem canoniza strings Typst. Vetor de rendimento: 12→1→0 esperado após a correção focal; não houve duas revisões sem ganho.
