{
  "created_at": "2026-09-09T10:51:09.491126+00:00",
  "regime": "A/B without technical isolation attestation",
  "pre_C": true,
  "cause": "Rustfmt check of exactly integrated frozen legacy successors; no semantic failure",
  "command": [
    "rustfmt",
    "--edition",
    "2021",
    "--emit",
    "stdout"
  ],
  "input": "01_core/src/compiler/eval/tests.rs (integrated TEST-ONLY source)",
  "input_sha256": "0289e083d8ffeb6b32730d8c4a487027ac2f57d82430601814f5ee513c2914dc",
  "formatted_sha256": "dd017795b6b9e9f8977d2ba2a2b3dac65979b6d7708f19bba747db4fa9e026a8",
  "input_body_sha256": "3e3483f9bc9f58eb24ee7c9431ad32e5ed6ad45c9312edc2bf347016daf84fe3",
  "formatted_body_sha256": "1e43fe499c5f1ef91ec79cc5e06d78c63105a841db0408381ea3a1737abe1947",
  "parent_frozen_replacements_sha256": "b58d880d85d0faf2ac0d204f2062e876e6897681ac645b42fb2b534932d2ffd3",
  "frozen_snippet_sha256": "e65d2e6dc6535983b0c5a63f4567c9aa9a6ddd9150a4e092310b1266b209099e",
  "semantic_argument": "Only deterministic rustfmt output in full integrated test context. String literals and numeric ranges remain byte-identical and ordered in every legacy successor. Rustfmt may introduce match-arm braces or trailing commas. No expected value, assertion, helper or case is revised.",
  "replacements": [
    {
      "old": "                        expression,\n                        features.clone(),\n                    );\n                    match *expression {\n                        \"{ import std; global }\" =>\n                            p1327_oracles::assert_warnings(expression, &side, &[9..12]),\n                        \"{ let renamed = std; import renamed; global }\" =>\n                            p1327_oracles::assert_warnings(expression, &side, &[28..35]),\n                        _ => assert!(side.is_empty(), \"{profile}/{expression}: {side:?}\"),\n                    }\n                    let diagnostics = result.expect_err(expression);\n                    assert_eq!(diagnostics.len(), 1, \"{profile}/{expression}\");\n",
      "new": "                        expression,\n                        features.clone(),\n                    );\n                    match *expression {\n                        \"{ import std; global }\" => {\n                            p1327_oracles::assert_warnings(expression, &side, &[9..12])\n                        }\n                        \"{ let renamed = std; import renamed; global }\" => {\n                            p1327_oracles::assert_warnings(expression, &side, &[28..35])\n                        }\n                        _ => assert!(side.is_empty(), \"{profile}/{expression}: {side:?}\"),\n                    }\n                    let diagnostics = result.expect_err(expression);\n                    assert_eq!(diagnostics.len(), 1, \"{profile}/{expression}\");\n"
    },
    {
      "old": "                        let (id, expression, expected) = &cases[index];\n                        let (result, side) =\n                            eval_expression_with_features(&world(), expression, features);\n                        match *id {\n                            \"global-bare-positive\" =>\n                                p1327_oracles::assert_warnings(expression, &side, &[9..12]),\n                            \"global-bare-alias-positive\" =>\n                                p1327_oracles::assert_warnings(expression, &side, &[26..31]),\n                            _ => assert!(side.is_empty(), \"{profile}/{id}: {side:?}\"),\n                        }\n                        assert_eq!(&result.expect(id), expected, \"{profile}/{id}\");\n                    }\n",
      "new": "                        let (id, expression, expected) = &cases[index];\n                        let (result, side) =\n                            eval_expression_with_features(&world(), expression, features);\n                        match *id {\n                            \"global-bare-positive\" => p1327_oracles::assert_warnings(\n                                expression,\n                                &side,\n                                &[9..12],\n                            ),\n                            \"global-bare-alias-positive\" => {\n                                p1327_oracles::assert_warnings(\n                                    expression,\n                                    &side,\n                                    &[26..31],\n                                )\n                            }\n                            _ => assert!(side.is_empty(), \"{profile}/{id}: {side:?}\"),\n                        }\n                        assert_eq!(&result.expect(id), expected, \"{profile}/{id}\");\n                    }\n"
    }
  ],
  "canonical_legacy_successors": [
    {
      "function": "p1305_global_bare",
      "observations": [
        "p1305_global_bare"
      ],
      "old": "        #[test]\n        fn p1305_global_bare() {\n            for (profile, features) in p1300_profiles() {\n                assert_eq!(\n                    observe(\n                        r###\"{ import std; (repr(std), std.calc.abs(-7)) }\"###,\n                        features\n                    ),\n                    Value::Array(vec![\n                        Value::Str(r###\"<module global>\"###.into()),\n                        Value::Int(7)\n                    ]),\n                    \"{profile}/global-bare\"\n                );\n            }\n        }\n\n",
      "new": "        #[test]\n        fn p1305_global_bare() {\n            for (profile, features) in p1300_profiles() {\n                assert_eq!(\n                    p1327_oracles::observe(\n                        r###\"{ import std; (repr(std), std.calc.abs(-7)) }\"###,\n                        features,\n                        &[9..12]\n                    ),\n                    Value::Array(vec![\n                        Value::Str(r###\"<module global>\"###.into()),\n                        Value::Int(7)\n                    ]),\n                    \"{profile}/global-bare\"\n                );\n            }\n        }\n\n"
    },
    {
      "function": "p1305_global_alias_bare",
      "observations": [
        "p1305_global_alias_bare"
      ],
      "old": "        #[test]\n        fn p1305_global_alias_bare() {\n            for (profile, features) in p1300_profiles() {\n                assert_eq!(\n                    observe(\n                        r###\"{ let renamed = std; import renamed; (repr(renamed), renamed.calc.abs(-8)) }\"###,\n                        features\n                    ),\n                    Value::Array(vec![\n                        Value::Str(r###\"<module global>\"###.into()),\n                        Value::Int(8)\n                    ]),\n                    \"{profile}/global-alias-bare\"\n                );\n            }\n        }\n\n",
      "new": "        #[test]\n        fn p1305_global_alias_bare() {\n            for (profile, features) in p1300_profiles() {\n                assert_eq!(\n                    p1327_oracles::observe(\n                        r###\"{ let renamed = std; import renamed; (repr(renamed), renamed.calc.abs(-8)) }\"###,\n                        features,\n                        &[28..35]\n                    ),\n                    Value::Array(vec![\n                        Value::Str(r###\"<module global>\"###.into()),\n                        Value::Int(8)\n                    ]),\n                    \"{profile}/global-alias-bare\"\n                );\n            }\n        }\n\n"
    },
    {
      "function": "p1305_import_binding_negatives_and_dynamic_spans",
      "observations": [
        "negative-global-bare",
        "negative-global-alias-bare"
      ],
      "old": "        #[test]\n        fn p1305_import_binding_negatives_and_dynamic_spans() {\n            let cases: [(&str, &str, std::ops::Range<usize>, &[&str]); 6] = [\n                (\"{ import std; global }\", \"unknown variable `global`\", 14..20, &[]),\n                (\n                    \"{ let renamed = std; import renamed; global }\",\n                    \"unknown variable `global`\",\n                    37..43,\n                    &[],\n                ),\n                (\n                    \"{ import \\\"ordinary/holder.typ\\\" as holder; import holder.saved; global }\",\n                    \"unknown variable `global`\",\n                    63..69,\n                    &[],\n                ),\n                (\n                    \"{ import (std); global }\",\n                    \"dynamic import requires an explicit name\",\n                    9..14,\n                    &[\"you can name the import with `as`\"],\n                ),\n                (\n                    \"{ import { std }; none }\",\n                    \"dynamic import requires an explicit name\",\n                    9..16,\n                    &[\"you can name the import with `as`\"],\n                ),\n                (\n                    \"{ let f() = std; import f(); none }\",\n                    \"dynamic import requires an explicit name\",\n                    24..27,\n                    &[\"you can name the import with `as`\"],\n                ),\n            ];\n            for (profile, features) in p1300_profiles() {\n                for (expression, message, span, hints) in &cases {\n                    let world = world(\"\");\n                    let source = Source::new_with_parser(\n                        world.main(),\n                        expression.to_string(),\n                        crate::compiler::parse::parse_code,\n                    );\n                    let (result, side) = eval_expression_with_features(\n                        &world,\n                        expression,\n                        features.clone(),\n                    );\n                    assert!(side.is_empty(), \"{profile}/{expression}: {side:?}\");\n                    let diagnostics = result.expect_err(expression);\n                    assert_eq!(diagnostics.len(), 1, \"{profile}/{expression}\");\n                    assert_eq!(\n                        diagnostics[0].message, *message,\n                        \"{profile}/{expression}\"\n                    );\n                    assert_eq!(\n                        diagnostics[0]\n                            .hints\n                            .iter()\n                            .map(|h| h.as_str())\n                            .collect::<Vec<_>>(),\n                        *hints,\n                        \"{profile}/{expression}\"\n                    );\n                    assert_eq!(\n                        source.span_byte_range(diagnostics[0].span),\n                        Some(span.clone()),\n                        \"{profile}/{expression}\"\n                    );\n                }\n            }\n        }\n\n",
      "new": "        #[test]\n        fn p1305_import_binding_negatives_and_dynamic_spans() {\n            let cases: [(&str, &str, std::ops::Range<usize>, &[&str]); 6] = [\n                (\"{ import std; global }\", \"unknown variable `global`\", 14..20, &[]),\n                (\n                    \"{ let renamed = std; import renamed; global }\",\n                    \"unknown variable `global`\",\n                    37..43,\n                    &[],\n                ),\n                (\n                    \"{ import \\\"ordinary/holder.typ\\\" as holder; import holder.saved; global }\",\n                    \"unknown variable `global`\",\n                    63..69,\n                    &[],\n                ),\n                (\n                    \"{ import (std); global }\",\n                    \"dynamic import requires an explicit name\",\n                    9..14,\n                    &[\"you can name the import with `as`\"],\n                ),\n                (\n                    \"{ import { std }; none }\",\n                    \"dynamic import requires an explicit name\",\n                    9..16,\n                    &[\"you can name the import with `as`\"],\n                ),\n                (\n                    \"{ let f() = std; import f(); none }\",\n                    \"dynamic import requires an explicit name\",\n                    24..27,\n                    &[\"you can name the import with `as`\"],\n                ),\n            ];\n            for (profile, features) in p1300_profiles() {\n                for (expression, message, span, hints) in &cases {\n                    let world = world(\"\");\n                    let source = Source::new_with_parser(\n                        world.main(),\n                        expression.to_string(),\n                        crate::compiler::parse::parse_code,\n                    );\n                    let (result, side) = eval_expression_with_features(\n                        &world,\n                        expression,\n                        features.clone(),\n                    );\n                    match *expression {\n                        \"{ import std; global }\" => {\n                            p1327_oracles::assert_warnings(expression, &side, &[9..12])\n                        }\n                        \"{ let renamed = std; import renamed; global }\" => {\n                            p1327_oracles::assert_warnings(expression, &side, &[28..35])\n                        }\n                        _ => assert!(side.is_empty(), \"{profile}/{expression}: {side:?}\"),\n                    }\n                    let diagnostics = result.expect_err(expression);\n                    assert_eq!(diagnostics.len(), 1, \"{profile}/{expression}\");\n                    assert_eq!(\n                        diagnostics[0].message, *message,\n                        \"{profile}/{expression}\"\n                    );\n                    assert_eq!(\n                        diagnostics[0]\n                            .hints\n                            .iter()\n                            .map(|h| h.as_str())\n                            .collect::<Vec<_>>(),\n                        *hints,\n                        \"{profile}/{expression}\"\n                    );\n                    assert_eq!(\n                        source.span_byte_range(diagnostics[0].span),\n                        Some(span.clone()),\n                        \"{profile}/{expression}\"\n                    );\n                }\n            }\n        }\n\n"
    },
    {
      "function": "p1306_existing_lookup_and_repr_all_profiles",
      "observations": [
        "global-bare-positive",
        "global-bare-alias-positive"
      ],
      "old": "        #[test]\n        fn p1306_existing_lookup_and_repr_all_profiles() {\n            let cases = [\n                (\"positive-std\", \"{ import \\\"ordinary/std.typ\\\" as specimen; (repr(specimen), specimen.x) }\", Value::Array(vec![Value::Str(\"<module std>\".into()), Value::Int(7)])),\n                (\"positive-global\", \"{ import \\\"ordinary/global.typ\\\" as specimen; (repr(specimen), specimen.x) }\", Value::Array(vec![Value::Str(\"<module global>\".into()), Value::Int(8)])),\n                (\"positive-map\", \"{ import \\\"ordinary/map.typ\\\" as specimen; (repr(specimen), specimen.x) }\", Value::Array(vec![Value::Str(\"<module map>\".into()), Value::Int(9)])),\n                (\"positive-aurora\", \"{ import \\\"ordinary/aurora.typ\\\" as specimen; (repr(specimen), specimen.x) }\", Value::Array(vec![Value::Str(\"<module aurora>\".into()), Value::Int(10)])),\n                (\"positive-reexport\", \"{ import \\\"reexport/std.typ\\\" as r; let renamed = r; (repr(r), repr(renamed), renamed.calc.abs(-12), repr(r.calc)) }\", Value::Array(vec![Value::Str(\"<module std>\".into()), Value::Str(\"<module std>\".into()), Value::Int(12), Value::Str(\"<module calc>\".into())])),\n                (\"positive-alias-shadow-nested\", \"{ import \\\"ordinary/std.typ\\\" as original; let alias = original; let std = alias; let box = (inner: std); (repr(alias), repr(std), box.inner.x, box.inner.twice(6)) }\", Value::Array(vec![Value::Str(\"<module std>\".into()), Value::Str(\"<module std>\".into()), Value::Int(7), Value::Int(12)])),\n                (\"positive-imported-nested\", \"{ import \\\"routes/holder.typ\\\" as holder; (repr(holder.saved), holder.saved.twice(11)) }\", Value::Array(vec![Value::Str(\"<module std>\".into()), Value::Int(22)])),\n                (\"global-bare-positive\", \"{ import std; (repr(std), std.calc.abs(-7)) }\", Value::Array(vec![Value::Str(\"<module global>\".into()), Value::Int(7)])),\n                (\"global-bare-alias-positive\", \"{ let named = std; import named; (repr(named), named.calc.abs(-8)) }\", Value::Array(vec![Value::Str(\"<module global>\".into()), Value::Int(8)])),\n                (\"global-import-forms\", \"{ import (std) as chosen; import std: calc as c; (repr(chosen), c.abs(-9)) }\", Value::Array(vec![Value::Str(\"<module global>\".into()), Value::Int(9)])),\n                (\"existing-lookups\", \"(repr(type(std.rgb)), calc.abs(-7), repr(type(sym.alpha)), repr(type(color.map.viridis)), repr(type(pdf.attach)), repr(type(pdf.artifact)))\", Value::Array(vec![Value::Str(\"function\".into()), Value::Int(7), Value::Str(\"symbol\".into()), Value::Str(\"array\".into()), Value::Str(\"function\".into()), Value::Str(\"function\".into())])),\n            ];\n            for reverse in [false, false, true] {\n                for (profile, features) in p1300_profiles() {\n                    for index in 0..cases.len() {\n                        let index = if reverse { cases.len() - 1 - index } else { index };\n                        let (id, expression, expected) = &cases[index];\n                        let (result, side) =\n                            eval_expression_with_features(&world(), expression, features);\n                        assert!(side.is_empty(), \"{profile}/{id}: {side:?}\");\n                        assert_eq!(&result.expect(id), expected, \"{profile}/{id}\");\n                    }\n                }\n            }\n        }\n\n",
      "new": "        #[test]\n        fn p1306_existing_lookup_and_repr_all_profiles() {\n            let cases = [\n                (\"positive-std\", \"{ import \\\"ordinary/std.typ\\\" as specimen; (repr(specimen), specimen.x) }\", Value::Array(vec![Value::Str(\"<module std>\".into()), Value::Int(7)])),\n                (\"positive-global\", \"{ import \\\"ordinary/global.typ\\\" as specimen; (repr(specimen), specimen.x) }\", Value::Array(vec![Value::Str(\"<module global>\".into()), Value::Int(8)])),\n                (\"positive-map\", \"{ import \\\"ordinary/map.typ\\\" as specimen; (repr(specimen), specimen.x) }\", Value::Array(vec![Value::Str(\"<module map>\".into()), Value::Int(9)])),\n                (\"positive-aurora\", \"{ import \\\"ordinary/aurora.typ\\\" as specimen; (repr(specimen), specimen.x) }\", Value::Array(vec![Value::Str(\"<module aurora>\".into()), Value::Int(10)])),\n                (\"positive-reexport\", \"{ import \\\"reexport/std.typ\\\" as r; let renamed = r; (repr(r), repr(renamed), renamed.calc.abs(-12), repr(r.calc)) }\", Value::Array(vec![Value::Str(\"<module std>\".into()), Value::Str(\"<module std>\".into()), Value::Int(12), Value::Str(\"<module calc>\".into())])),\n                (\"positive-alias-shadow-nested\", \"{ import \\\"ordinary/std.typ\\\" as original; let alias = original; let std = alias; let box = (inner: std); (repr(alias), repr(std), box.inner.x, box.inner.twice(6)) }\", Value::Array(vec![Value::Str(\"<module std>\".into()), Value::Str(\"<module std>\".into()), Value::Int(7), Value::Int(12)])),\n                (\"positive-imported-nested\", \"{ import \\\"routes/holder.typ\\\" as holder; (repr(holder.saved), holder.saved.twice(11)) }\", Value::Array(vec![Value::Str(\"<module std>\".into()), Value::Int(22)])),\n                (\"global-bare-positive\", \"{ import std; (repr(std), std.calc.abs(-7)) }\", Value::Array(vec![Value::Str(\"<module global>\".into()), Value::Int(7)])),\n                (\"global-bare-alias-positive\", \"{ let named = std; import named; (repr(named), named.calc.abs(-8)) }\", Value::Array(vec![Value::Str(\"<module global>\".into()), Value::Int(8)])),\n                (\"global-import-forms\", \"{ import (std) as chosen; import std: calc as c; (repr(chosen), c.abs(-9)) }\", Value::Array(vec![Value::Str(\"<module global>\".into()), Value::Int(9)])),\n                (\"existing-lookups\", \"(repr(type(std.rgb)), calc.abs(-7), repr(type(sym.alpha)), repr(type(color.map.viridis)), repr(type(pdf.attach)), repr(type(pdf.artifact)))\", Value::Array(vec![Value::Str(\"function\".into()), Value::Int(7), Value::Str(\"symbol\".into()), Value::Str(\"array\".into()), Value::Str(\"function\".into()), Value::Str(\"function\".into())])),\n            ];\n            for reverse in [false, false, true] {\n                for (profile, features) in p1300_profiles() {\n                    for index in 0..cases.len() {\n                        let index = if reverse { cases.len() - 1 - index } else { index };\n                        let (id, expression, expected) = &cases[index];\n                        let (result, side) =\n                            eval_expression_with_features(&world(), expression, features);\n                        match *id {\n                            \"global-bare-positive\" => p1327_oracles::assert_warnings(\n                                expression,\n                                &side,\n                                &[9..12],\n                            ),\n                            \"global-bare-alias-positive\" => {\n                                p1327_oracles::assert_warnings(\n                                    expression,\n                                    &side,\n                                    &[26..31],\n                                )\n                            }\n                            _ => assert!(side.is_empty(), \"{profile}/{id}: {side:?}\"),\n                        }\n                        assert_eq!(&result.expect(id), expected, \"{profile}/{id}\");\n                    }\n                }\n            }\n        }\n\n"
    }
  ],
  "full_diff": "--- integrated-tests.rs\n+++ formatted-tests.rs\n@@ -19593,10 +19593,12 @@\n                         features.clone(),\n                     );\n                     match *expression {\n-                        \"{ import std; global }\" =>\n-                            p1327_oracles::assert_warnings(expression, &side, &[9..12]),\n-                        \"{ let renamed = std; import renamed; global }\" =>\n-                            p1327_oracles::assert_warnings(expression, &side, &[28..35]),\n+                        \"{ import std; global }\" => {\n+                            p1327_oracles::assert_warnings(expression, &side, &[9..12])\n+                        }\n+                        \"{ let renamed = std; import renamed; global }\" => {\n+                            p1327_oracles::assert_warnings(expression, &side, &[28..35])\n+                        }\n                         _ => assert!(side.is_empty(), \"{profile}/{expression}: {side:?}\"),\n                     }\n                     let diagnostics = result.expect_err(expression);\n@@ -19778,10 +19780,18 @@\n                         let (result, side) =\n                             eval_expression_with_features(&world(), expression, features);\n                         match *id {\n-                            \"global-bare-positive\" =>\n-                                p1327_oracles::assert_warnings(expression, &side, &[9..12]),\n-                            \"global-bare-alias-positive\" =>\n-                                p1327_oracles::assert_warnings(expression, &side, &[26..31]),\n+                            \"global-bare-positive\" => p1327_oracles::assert_warnings(\n+                                expression,\n+                                &side,\n+                                &[9..12],\n+                            ),\n+                            \"global-bare-alias-positive\" => {\n+                                p1327_oracles::assert_warnings(\n+                                    expression,\n+                                    &side,\n+                                    &[26..31],\n+                                )\n+                            }\n                             _ => assert!(side.is_empty(), \"{profile}/{id}: {side:?}\"),\n                         }\n                         assert_eq!(&result.expect(id), expected, \"{profile}/{id}\");\n"
}
