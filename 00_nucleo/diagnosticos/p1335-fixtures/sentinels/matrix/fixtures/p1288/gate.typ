#set page(width: 180pt, height: 120pt, margin: 10pt)
#table(
  columns: 2,
  pdf.header-cell(scope: "both")[Header],
  pdf.header-cell(scope: "column")[Value],
  pdf.header-cell(scope: "row")[Row],
  pdf.data-cell[Data],
)
