# P1336 — revisão do freeze de testes anterior a C

Veredicto: freeze A/B suficiente para o fragmento Int/Str contratado; a implementação continua condicionada ao RED local real. Nenhum candidato foi avaliado neste recibo.

Manifesto `54864bfd67d50197560011d5aae055a0303fcc9cadc4230221dfd4f7a6daea88`; freeze `ba3f1a49fb0f96dc0770336274f230f4c2dde5ccf390c770c03a8b3244098cfa`, UTC `2026-09-09T18:11:29.298737+00:00`; baseline de testes `eb814dcad2572dc79205b2489f5d81f82637233d6a11def5fdb8542e965ffc26`; expectativas `502f23e789dde6ab4c698a5e5212cf6892f41cb20548bdb504a6c00767385073`. Proveniência completa da árvore não commitada, HEAD e diff/stat está em `p1336-tests-baseline.json`. Hash conferido do módulo Rust independente: `b30cc9449bef081d51d44d664aed04c4c26c48bcde15ecf4b31938a1ecfd9934`.

Inspecionei catálogo, runner, expectativas, saídas e módulo local. Os cinco testes locais comparam nomes e spans fornecidos no lookup puro, ranges reais de bytes da AST, severidade, cardinalidade, hints/traces/sink e sucessos. Variam valores, nomes de fields, Unicode, parênteses, alias e deslocamentos. O comparador CLI usa exit e stdout/stderr integrais em base64, sem normalizar mensagens ou remover canais. O auditor mantém Unknown para transportes inválidos, crash, bytes incoerentes e JSON inválido; opacidade é controle explícito do harness, sem crédito de produto.

As 1080 observações abrangem 45 fontes, quatro perfis, três ordens e dois executáveis. Todas são Observed, fontes correspondem ao catálogo final e os vetores são estáveis. Há 180 expectativas por caso/perfil: 52 de convergência obrigatória, 88 de paridade preservada e 40 de dívida preservada. São contagens dos artefatos identificados acima, não inferência de paridade geral.

A calibração pré-C de `"abc".len` é legítima: a hipótese inicial de convergência foi refutada pelo sucesso anterior ao lookup. L0 exclui esse pré-despacho; a expectativa correta preserva dívida. Não houve mudança de fonte ou dos canais observados. O recibo `p1336-tests-calibration.md` preserva o incidente e informa a repetição redundante que parou na guarda contra sobrescrita. Não se transfere esse caso para paridade, nem se adapta ao candidato. O lookup puro Str/len continua testando sua obrigação própria.

O runner e o catálogo mudaram antes do freeze final para finalizar a classificação a partir do baseline já observado. O freeze protege os hashes finais; o baseline conserva seus hashes históricos. Isso exige distinguir calibração anterior a C de alteração após freeze. O módulo local foi formatado e pinado antes de C; futuras mudanças nele invalidariam a alegação de testes congelados.

Os controles CLI incluem Type, Bool/Array/None/Auto, Module, Dict, Content raw, Float, funções nativas/closures/With, PDF e features, métodos e erros antecipados. LocatedContent será complementado pelo teste legado `p1325_located_ast_preserves_whole_access_for_snapshot_and_legacy`. Warning causal possui o teste legado `p820_sym_join_field_access_warning_depreciacao`. Não há cobertura A/B específica da leitura contextual text.size/lang; sua preservação depende de diff estático do ramo e gates workspace, e esse limite deve constar do fechamento. Essa combinação é proporcional à alteração local, sem afirmar equivalência de todas as superfícies excluídas.

Os ataques congelados distinguem regressões nos dois nomes e nos dois spans, extrapolação a Bool e confusão com namespaces Type. A validade e a eliminação de cada mutante ainda serão julgadas depois da execução. Nenhum score anterior substitui esse gate.
