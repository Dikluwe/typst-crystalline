# Sucessão cega V3 do recorte observável

Esta emenda sucede apenas a fronteira dos dois positivos Table descritos em `p1324-ab-obligation.md`. Parecer separado autorizador: `p1324-review-ab-reopen.md`, SHA 81ec01d622f94064c0c2d91e52fc4439f152df3b66e3f9d61c994b1f04c29469. Nenhum caso anterior é apagado; nenhum negativo nativo, trace, comparador ou política de Unknown muda. O snippet independente permanece byte-idêntico ao freeze unitário original.

V3 é posterior à existência de C: root comunicou que aplicou C depois do freeze V2 e antes do aviso sobre Content `.body`. Este autor não leu source/diff/resultado de C, nem o executou. V3 decorre somente de L0 e medições dos binários oracle/baseline anteriores ao candidato. Não alegar freeze integral anterior à existência de C, isolamento atestado ou selo.

Os dois literais `table-present-call` e `with-present-call` continuam no corpus, agora como preservação baseline=C de Content. Suas expectativas de forma documentam success no oracle e error no baseline/candidato, sem conceder crédito de paridade. Dois positivos novos observam `(type(cell), repr(cell), type(cell[ok]))`, na forma direta e por With aninhado, cobrindo descoberta, kind, nome público e execução real sem acessar outro field do Content retornado.

Medição focal anterior a C observado: 16 processos vanilla/0.20011983800213784 s e 16 baseline/1.5805115120019764 s, com fontes/argv/UTC/HEAD/stat nos recibos focais v3. Os oito novos pares dos quatro perfis coincidem exatamente em exit 0, stderr vazio, stdout `["function","cell","content"]` com newline. Os oito pares das duas fixtures antigas repetem exatamente seus observáveis próprios retidos em V2. Ganho: separa preservação de Content de sucesso efetivo da chamada nativa, acrescentando oito comparações positivas válidas, sem alterar produto. Não é uma segunda revisão sem ganho da causa instrumental V2.

Os índices `p1324-ab-oracle-v3.json` e `p1324-ab-baseline-v3.json` combinam os recibos anteriores com os focais novos; não representam novos processos. Cada índice tem 200 vetores de 50 casos nos quatro perfis. Os recibos componentes conservam as execuções e a proveniência. Na coleta paralela baseline focal v3, a referência v2 não continha os dois novos casos, portanto oito comparações apareceram como Unknown por referência ausente; os canais públicos completos foram retidos e essas comparações são explicitamente recomputadas no índice contra o oracle focal v3. Não há conversão implícita de Unknown em sucesso; o dado público faltante foi fornecido antes do freeze e antes de execução de C.

O runner v3 deriva de v2 somente por caminhos de novos artefatos, seleção `--ids` para a calibração dos novos casos e metadata de forma oracle/baseline nas duas fixtures antigas. `--ids` é proibido para C e não evita seu freeze. A função de comparação e a política de Unknown são byte-idênticas a V2. Stderr continua integral, sem normalização. C deve passar focal (nove casos × quatro perfis = 36 processos), depois normal/repeat/reverse integral (50 × 4 × 3 = 600 processos), com zero obrigação Unknown, diferença obrigatória ou instabilidade. Os controles têm referência baseline, os 30 casos do recorte de paridade têm referência vanilla.

As perdas de captura/argv de V1/V2 estão em `p1324-ab-instrumentation-v2.md` e `p1324-ab-baseline-findings.md`; os vermelhos antigos são preservados. Esta emenda registra método e evidência; somente o revisor separado julga o resultado final.
