# P1322 — D, objeção causal ao primeiro anexo dos canais

Em 2026-09-08T22:55:02Z, a conferência independente `p1322-review-channels.json` já confirmou 222 linhas executadas e 51 MATCH de projeção com canais diferentes, sem instabilidade. Esta objeção não invalida os dados nem modifica qualquer input; bloqueia somente a seleção conclusiva do relatório até decisão nova.

D leu integralmente `wiring.md` (490 linhas), `shell/diagnostic.md` e o núcleo `wiring/cli-observables.toml`, além da fonte bilateral e do primeiro `p1322-classification-query-hint-addendum.md`. Em `wiring.md:172–173`, o owner deve emitir o **warning experimental medido no vanilla**. O objeto medido em `lab/typst-original/crates/typst/src/lib.rs:249–256` inclui headline e três hints. `04_wiring/src/main.rs:388–392` emite somente a headline. As saídas preservadas confirmam o delta em duas grafias de compile, dois perfis habilitados e três ordens.

A primeira classificação C reconhece a ausência dos hints, mas atribui prioridade 3 por não haver uma cláusula que enumere literalmente cada texto. D rejeita esse estreitamento: warning medido não significa somente headline; P1322 exige precisamente preservar hints e diagnósticos completos. A indicação contratual é específica ao warning HTML, não somente uma promessa genérica de paridade. Sem uma fronteira normativa que limite explicitamente esse warning à headline, há contradição L0 em rota canônica: prioridade 2.

Se o conjunto de owners e a superfície restrita alegados por C forem sustentáveis (emissão fixa existente; um owner; um path causal), `html-experimental-warning-hints` precede qualquer coorte prioridade 3, inclusive `namespace-function-missing-field`. Se a composição L4 exigir owner adicional/API pública, deve-se rever owners/gate, nunca rebaixar a prioridade para preservar a escolha antiga. Dois IDs/duas grafias não são dois paths causais.

Solicitado sucessor ou refutação normativa específica, preservando o primeiro anexo. A revisão anterior da seleção permanece válida apenas sobre seu conjunto congelado incompleto anterior ao overlay; não autoriza a seleção final após essa descoberta. Não há mudança de produto/L0, nem ampliação de denominador, nem crédito adicional no score único 13/13 do auditor.

Pin do primeiro anexo lido: SHA-256 `cca16ba1b8ad9f4776fdfd49ca5236641c056fb17f249681c76da5e198604381`. O timestamp inicial digitado antes do primeiro handoff foi corrigido imediatamente pela leitura do relógio UTC; este documento só passa a congelado após esse ajuste editorial.
